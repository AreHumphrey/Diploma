% ============================================================
% ПРИЛОЖЕНИЯ
% ============================================================

\appendix
\renewcommand{\thesection}{Приложение~\Asbuk{section}}

% ============================================================
\newpage
\section{Приложение А}
\phantomsection
\label{app:features}
\addcontentsline{toc}{section}{Приложение А~--- Перечень признаков модели}
\begin{center}
\textbf{ПЕРЕЧЕНЬ ПРИЗНАКОВ МОДЕЛИ}
\end{center}

\vspace{1cm}

\begin{table}[h]
\centering
\caption{Структура признаков, используемых в модели прогнозирования}
\label{tab:features}
\begin{tabular}{|l|l|l|p{6cm}|}
\hline
\textbf{Признак} & \textbf{Тип} & \textbf{Диапазон} & \textbf{Описание} \\
\hline
driver\_age & Числовой & [18; 80] & Возраст водителя в годах \\
\hline
driver\_experience & Числовой & [0; 60] & Водительский стаж в годах \\
\hline
vehicle\_age & Числовой & [0; 30] & Возраст транспортного средства \\
\hline
engine\_power & Числовой & [60; 300] & Мощность двигателя (л.с.) \\
\hline
num\_claims & Числовой & [0; 10] & Количество страховых обращений \\
\hline
violation\_count & Числовой & [0; 20] & Количество нарушений ПДД \\
\hline
pct\_days\_with\_rain & Числовой & [0; 1] & Доля дней с осадками \\
\hline
night\_driving\_ratio & Числовой & [0; 1] & Доля ночных поездок \\
\hline
vehicle\_type & Категориальный & --- & Тип транспортного средства \\
\hline
region & Категориальный & --- & Регион эксплуатации \\
\hline
occupation\_type & Категориальный & --- & Тип занятости водителя \\
\hline
target & Бинарный & \{0, 1\} & Факт страхового случая \\
\hline
\end{tabular}
\end{table}

% ============================================================
\newpage
\section{Приложение Б}
\phantomsection
\label{app:feature_code}
\addcontentsline{toc}{section}{Приложение Б~--- Генерация признаков и обучение модели}
\begin{center}
\textbf{ГЕНЕРАЦИЯ ПРИЗНАКОВ И ОБУЧЕНИЕ МОДЕЛИ}
\end{center}

\vspace{1cm}

\begin{lstlisting}[
    caption={Код генерации производных признаков}, 
    label={lst:feature_eng},
    breaklines=true,
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]
import pandas as pd
import numpy as np

def generate_features(df: pd.DataFrame) -> pd.DataFrame:
    df = df.copy()

    df['age_squared'] = df['driver_age'] ** 2
    df['experience_ratio'] = df['driver_experience'] / (df['driver_age'] + 1e-5)
    df['claims_per_year'] = df['num_claims'] / (df['driver_experience'] + 1e-5)
    df['violations_per_year'] = df['violation_count'] / (df['driver_experience'] + 1e-5)

    df['no_claims_long'] = ((df['num_claims'] == 0) & (df['driver_experience'] >= 5)).astype(int)
    df['experienced_clean'] = (
            (df['driver_experience'] >= 10) &
            (df['num_claims'] == 0) &
            (df['violation_count'] == 0)
    ).astype(int)

    df['young_risky'] = ((df['driver_age'] < 26) & (df['violation_count'] > 0)).astype(int)
    df['high_claims'] = (df['num_claims'] >= 2).astype(int)

    df['night_x_trips'] = df['night_driving_ratio'] * df['avg_trips_per_week']
    df['power_x_age'] = df['engine_power'] * df['vehicle_age']

    return df
\end{lstlisting}

% ============================================================
\newpage
\section{Приложение В}
\phantomsection
\label{app:training}
\addcontentsline{toc}{section}{Приложение В~--- Обработка признаков и обучение CatBoost}
\begin{center}
\textbf{ОБРАБОТКА ПРИЗНАКОВ И ОБУЧЕНИЕ CATBOOST}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Обучение модели CatBoostClassifier}, label={lst:training}
    caption={Код генерации производных признаков}, 
    label={lst:feature_eng},
    breaklines=true,
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]
def train(self, data: pd.DataFrame, labels: pd.Series):
        processed = self.preprocess(data)

        X_train, X_val, y_train, y_val = train_test_split(
            processed, labels, test_size=0.2, stratify=labels, random_state=42
        )

        classes = np.unique(y_train)
        class_weights = compute_class_weight('balanced', classes=classes, y=y_train)
        weight_dict = dict(zip(classes, class_weights))

        self.model = CatBoostClassifier(
            verbose=100,
            iterations=1500,
            learning_rate=0.03,
            depth=5,
            eval_metric='AUC',
            early_stopping_rounds=100,
            random_seed=42,
            class_weights=weight_dict
        )

        self.model.fit(
            X_train, y_train,
            eval_set=(X_val, y_val),
            cat_features=self.cat_features,
            verbose=100
        )

        y_proba = self.model.predict_proba(X_val)[:, 1]

        best_threshold = 0.5
        best_f1 = 0

        for th in np.arange(0.05, 0.4, 0.01):
            y_pred = (y_proba >= th).astype(int)
            rec = recall_score(y_val, y_pred)
            if rec >= 0.6:
                f1 = f1_score(y_val, y_pred)
                if f1 > best_f1:
                    best_f1 = f1
                    best_threshold = th

        if best_f1 == 0:
            for th in np.arange(0.05, 0.5, 0.01):
                y_pred = (y_proba >= th).astype(int)
                f1 = f1_score(y_val, y_pred)
                if f1 > best_f1:
                    best_f1 = f1
                    best_threshold = th

        self.threshold = best_threshold
        print(f"Оптимальный порог: {self.threshold:.3f}, F1: {best_f1:.4f}")

\end{lstlisting}

% ============================================================
\newpage
\section{Приложение Г}
\phantomsection
\label{app:training}
\addcontentsline{toc}{section}{Приложение Г~--- Подбор оптимального порога классификации}
\begin{center}
\textbf{ПОДБОР ОПТИМАЛЬНОГО ПОРОГА КЛАССИФИКАЦИИ}
\end{center}

\vspace{1cm}

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{pictures/threshold_analysis.png}
\caption{Зависимость метрик качества (Precision, Recall, F1-score) от порога классификации $\theta$}
\label{fig:threshold}
\end{figure}

% ============================================================
\newpage
\section{Приложение Д}
\phantomsection
\label{app:save_model}
\addcontentsline{toc}{section}{Приложение Д~--- Сериализация модели}
\begin{center}
\textbf{СЕРИАЛИЗАЦИЯ МОДЕЛИ}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Метод сохранения модели в формат .cbm}, label={lst:save}    caption={Код генерации производных признаков}, 
    label={lst:feature_eng},
    breaklines=true,
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]

 def save_model(self, path: str):
        os.makedirs(os.path.dirname(path), exist_ok=True)
        if path.endswith('.cbm'):
            self.model.save_model(path)
        else:
            joblib.dump(self.model, path)

\end{lstlisting}


% ============================================================
\newpage
\section{Приложение Е}
\phantomsection
\label{app:importance}
\addcontentsline{toc}{section}{Приложение Е~--- Важность признаков}
\begin{center}
\textbf{ВАЖНОСТЬ ПРИЗНАКОВ МОДЕЛИ}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{pictures/metric_p.png}
\caption{Ранжирование признаков по степени влияния на прогноз}
\label{fig:importance}
\end{figure}

% ============================================================
\newpage
\section{Приложение Ж}
\phantomsection
\label{app:confusion}
\addcontentsline{toc}{section}{Приложение Ж~--- Матрица ошибок}
\begin{center}
\textbf{МАТРИЦА ОШИБОК (CONFUSION MATRIX)}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{pictures/matrix_err.png}
\caption{Распределение прогнозов модели}
\label{fig:confusion}
\end{figure}

% ============================================================
\newpage
\section{Приложение З}
\phantomsection
\label{app:roc}
\addcontentsline{toc}{section}{Приложение З~--- ROC-кривая}
\begin{center}
\textbf{ROC-КРИВАЯ МОДЕЛИ}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{pictures/roc_k.png}
\caption{ROC-кривая классификатора}
\label{fig:roc}
\end{figure}

% ============================================================
\newpage
\section{Приложение И}
\phantomsection
\label{app:importance_detailed}
\addcontentsline{toc}{section}{Приложение И~--- Детальный анализ важности признаков}
\begin{center}
\textbf{ДЕТАЛЬНЫЙ АНАЛИЗ ВАЖНОСТИ ПРИЗНАКОВ}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.9\textwidth]{pictures/v_p_p.png}
\caption{Вклад групп признаков в прогноз модели}
\label{fig:importance_detailed}
\end{figure}

% ============================================================
\newpage
\section{Приложение К}
\phantomsection
\label{app:colors}
\addcontentsline{toc}{section}{Приложение К~--- Цветовая палитра приложения}
\begin{center}
\textbf{ЦВЕТОВАЯ ПАЛИТРА ВЕБ-ПРИЛОЖЕНИЯ}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.4\textwidth]{pictures/scr_color.png}
\caption{Фирменная цветовая схема веб-приложения}
\label{fig:colors}
\end{figure}

% ============================================================
\newpage
\section{Приложение Л}
\phantomsection
\label{app:main_page}
\addcontentsline{toc}{section}{Приложение Л~--- Главная страница}
\begin{center}
\textbf{ГЛАВНАЯ СТРАНИЦА ВЕБ-ПРИЛОЖЕНИЯ}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{pictures/d_home_page.pdf}
\caption{Интерфейс главной страницы веб-приложения}
\label{fig:main_page}
\end{figure}

% ============================================================
\newpage
\section{Приложение М}
\phantomsection
\label{app:docs_page}
\addcontentsline{toc}{section}{Приложение М~--- Страница документации}
\begin{center}
\textbf{СТРАНИЦА ДОКУМЕНТАЦИИ}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{pictures/d_docx_page.pdf}
\caption{Страница с описанием методологии расчета}
\label{fig:docs_page}
\end{figure}

% ============================================================
\newpage
\section{Приложение Н}
\phantomsection
\label{app:form_page}
\addcontentsline{toc}{section}{Приложение Н~--- Форма ввода данных}
\begin{center}
\textbf{ФОРМА ВВОДА ПЕРСОНАЛЬНЫХ ДАННЫХ}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{pictures/scr_form.png}
\caption{Интерфейс ввода параметров водителя и транспортного средства}
\label{fig:form_page}
\end{figure}

% ============================================================
\newpage
\section{Приложение О}
\phantomsection
\label{app:result_page}
\addcontentsline{toc}{section}{Приложение О~--- Страница результатов}
\begin{center}
\textbf{СТРАНИЦА РЕЗУЛЬТАТОВ РАСЧЕТА}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{pictures/d_res_page.pdf}
\caption{Отображение финального тарифа и коэффициентов КБМ}
\label{fig:result_page}
\end{figure}

% ============================================================
\newpage
\section{Приложение П}
\phantomsection
\label{app:flask_init}
\addcontentsline{toc}{section}{Приложение П~--- Инициализация Flask-приложения}
\begin{center}
\textbf{ИНИЦИАЛИЗАЦИЯ FLASK-ПРИЛОЖЕНИЯ}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Основной модуль приложения (app.py)}, label={lst:flask_init}    label={lst:feature_eng},
    breaklines=true,
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]

from flask import Flask, render_template, request, redirect, url_for, session
import pandas as pd
import json
import os
from datetime import datetime
from src.models.hybrid.kbm_calculator import HybridKBMCalculator
from src.utils.dtc_checker import check_dtc_in_file
from flask import send_file
import io
import csv
import matplotlib
matplotlib.use('Agg') 
import matplotlib.pyplot as plt
import numpy as np
from flask_session import Session  

app = Flask(__name__)

app.config['SECRET_KEY'] = 'osago-secret-key-change-in-prod'  
app.config['SESSION_TYPE'] = 'filesystem'  
app.config['SESSION_FILE_DIR'] = './flask_session_data'
app.config['SESSION_PERMANENT'] = False
app.config['SESSION_USE_SIGNER'] = True
Session(app)


os.makedirs(app.config['SESSION_FILE_DIR'], exist_ok=True)

MODEL_PATH = "outputs/insurance_model_v1.cbm"
REGIONS_JSON = "static/data/regions.json"
UPLOAD_FOLDER = "static/uploads"

os.makedirs(UPLOAD_FOLDER, exist_ok=True)
\end{lstlisting}

% ============================================================
\newpage
\section{Приложение Р}
\phantomsection
\label{app:routes}
\addcontentsline{toc}{section}{Приложение Р~--- Маршруты сервера}
\begin{center}
\textbf{МАРШРУТЫ СЕРВЕРНОГО ПРИЛОЖЕНИЯ}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Реализация эндпоинтов Flask}, label={lst:routes}    breaklines=true,
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]
@app.route("/")
def index():
    return render_template("index.html")


@app.route("/docs")
def docs():
    return render_template("docs.html")


@app.route("/calculate", methods=["GET", "POST"])
def calculate():
    if request.method == "POST":
        print(f" POST-запрос на /calculate")
    
    regions_data = load_regions_data()

    if request.method == "GET":
        return render_template("calculate.html", regions=list(regions_data.keys()))

    if not calculator:
        print("Model not initialized")
        return "Model not loaded. Please restart the server.", 503
    
    try:
        dtc_file = request.files.get('dtc_file')
        has_dtc = False
        obd_file_path = None
        
        if dtc_file and dtc_file.filename and dtc_file.filename.lower().endswith('.csv'):
            try:
                timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
                filename = f"dtc_{timestamp}.csv"
                obd_file_path = os.path.join(UPLOAD_FOLDER, filename)
                dtc_file.save(obd_file_path)
                print(f"Файл сохранён: {obd_file_path}")
                has_dtc = check_dtc_in_file(obd_file_path)
                print(f"DTC-анализ: ошибки = {has_dtc}")
            except Exception as e:
                print(f" Ошибка обработки DTC-файла: {e}")
                has_dtc = False

        region_name = request.form.get("region", "Other")
        weather = regions_data.get(region_name, regions_data.get("Other", {}))

        driver_age = calculate_age(request.form.get("driver_dob"))
        driver_experience = calculate_experience(request.form.get("license_date"))

        try:
            vehicle_year = int(request.form.get("vehicle_year", 2020))
            vehicle_age = max(0, min(30, datetime.now().year - vehicle_year))
        except (ValueError, TypeError):
            vehicle_year = 2020
            vehicle_age = 5

        num_claims = safe_int(request.form.get("num_claims"), 0, 0, 20)
        violation_count = safe_int(request.form.get("violation_count"), 0, 0, 50)
        
        base_kbm = calculate_base_kbm(num_claims, driver_experience)


        night_driving = 0.5 if request.form.get("night_driving") == "yes" else 0.1
        is_owner = request.form.get("is_owner") == "true"
        unlimited_drivers = request.form.get("unlimited_drivers") == "true"

        case_data = {
            'driver_age': driver_age,
            'driver_experience': driver_experience,
            'vehicle_age': vehicle_age,
            'vehicle_type': request.form.get("body_type", "sedan"),
            'engine_power': safe_int(request.form.get("engine_power"), 150, 50, 500),
            'vehicle_purpose': request.form.get("vehicle_purpose", "personal"),
            'region': region_name,
            'pct_days_with_snow': weather.get('pct_days_with_snow', 0.3),
            'pct_days_with_rain': weather.get('pct_days_with_rain', 0.3),
            'winter_duration_months': weather.get('winter_duration_months', 4),
            'base_kbm': base_kbm,
            'num_claims': num_claims,
            'violation_count': violation_count,

            'occupation_type': request.form.get("occupation_type", "office_worker"),

            'night_driving_ratio': night_driving,
            'ko_multiplier': 1.8 if unlimited_drivers else 1.0,
            'num_owned_vehicles': 1 if is_owner else 0,

        }

        result_df = calculator.calculate(
            cases=[case_data],
            obd_file_path=obd_file_path if has_dtc else None,
            show_plot=False
        )
        
        result_row = result_df.iloc[0].to_dict()
        
        result = {
            'tariff': result_row.get('Итоговый КБМ', 0) * 2000,
            'final_kbm': result_row.get('Итоговый КБМ'),
            'base_kbm': base_kbm,
            'recommended_kbm': result_row.get('Рекомендуемый КБМ'),
            'accident_proba': result_row.get('Вероятность ДТП'),
            'adjustments': result_row.get('Корректировки', 'нет'),
            'region': region_name,
            'has_dtc': has_dtc,
            'driver_desc': case_data['description'],
            'num_claims': num_claims,
            'violation_count': violation_count,
            # Доп. поля для экспорта
            'driver_age': driver_age,
            'driver_experience': driver_experience,
            'vehicle_age': vehicle_age,
            'engine_power': case_data['engine_power'],
        }

        session['last_calculation'] = result
        print(f"Результат сохранён в сессию")
    

        
        return render_template(
            "result.html",
            result=result,
            input_data=case_data,
            has_dtc=has_dtc
        )
        
    except Exception as e:
        import traceback
        error_trace = traceback.format_exc()
        print(f"Критическая ошибка в /calculate: {e}")
        print(error_trace)
        
        return render_template(
            "result.html",
            result={'error': str(e)},
            input_data={},
            has_dtc=False,
            error_trace=error_trace if app.debug else None
        ), 500



\end{lstlisting}


% ============================================================
\newpage
\section{Приложение С}
\phantomsection
\label{app:post_handling}
\addcontentsline{toc}{section}{Приложение С~--- Обработка POST-запросов}
\begin{center}
\textbf{ОБРАБОТКА POST-ЗАПРОСОВ}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Обработка данных формы калькулятора}, label={lst:post}    
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]

def calculate_age(dob_str):
    if not dob_str:
        return 25
    try:
        dob = datetime.strptime(dob_str, "%Y-%m-%d")
        age = datetime.now().year - dob.year
        if (datetime.now().month, datetime.now().day) < (dob.month, dob.day):
            age -= 1
        return max(18, min(80, age))
    except:
        return 25


def calculate_experience(license_date_str):
    if not license_date_str:
        return 0
    try:
        l_date = datetime.strptime(license_date_str, "%Y-%m-%d")
        years = (datetime.now() - l_date).days / 365.25
        return max(0, min(60, int(years)))
    except:
        return 0


def calculate_base_kbm(num_claims: int, driver_experience: int) -> float:
    kbm = 1.0
    claim_free_years = max(0, driver_experience - num_claims * 2)
    discount = min(0.05 * claim_free_years, 0.54)
    kbm -= discount
    penalty = num_claims * 0.5
    kbm += penalty
    return round(max(0.46, min(3.92, kbm)), 2)

\end{lstlisting}

% ============================================================
\newpage
\section{Приложение Т}
\phantomsection
\label{app:export}
\addcontentsline{toc}{section}{Приложение Т~--- Функции экспорта}
\begin{center}
\textbf{ФУНКЦИИ ЭКСПОРТА РЕЗУЛЬТАТОВ}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Экспорт в PDF, CSV и PNG}, label={lst:export}    
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]

@app.route("/download/pdf")
def download_pdf():

    from reportlab.lib import colors
    from reportlab.lib.pagesizes import A4

    from reportlab.lib.styles import getSampleStyleSheet, ParagraphStyle
    from reportlab.lib.units import cm

    result = session.get('last_calculation')
    
    if not result:
        return "No calculation result found. Please calculate first.", 400
    
    buffer = io.BytesIO()

    elements = []
    
    styles = getSampleStyleSheet()

    elements.append(Paragraph("OSAGOCalculator — Result", title_style))
    elements.append(Spacer(1, 0.5*cm))
    
    data = [
        ['Parameter', 'Value'],
        ['Final CTP Tariff', f"{result['tariff']:.0f} ₽"],
        ['Final KBM', f"{result['final_kbm']:.2f}"],
        ['Base KBM', f"{result['base_kbm']:.2f}"],
        ['Region', result['region']],
        ['Driver', result['driver_desc']],
        ['Accidents', str(result['num_claims'])],
        ['DTC Errors', 'Yes' if result['has_dtc'] else 'No'],
    ]
    
    table = Table(data, colWidths=[6*cm, 6*cm])
    table.setStyle(TableStyle([
        ('BACKGROUND', (0, 0), (-1, 0), colors.HexColor('#00664E')),
        ('TEXTCOLOR', (0, 0), (-1, 0), colors.whitesmoke),
        ('ALIGN', (0, 0), (-1, -1), 'CENTER'),
        ('FONTNAME', (0, 0), (-1, 0), 'Helvetica-Bold'),
        ('FONTSIZE', (0, 0), (-1, 0), 14),
        ('BOTTOMPADDING', (0, 0), (-1, 0), 12),
        ('BACKGROUND', (0, 1), (-1, -1), colors.HexColor('#14281E')),
        ('TEXTCOLOR', (0, 1), (-1, -1), colors.whitesmoke),
        ('GRID', (0, 0), (-1, -1), 1, colors.HexColor('#00D46A')),
        ('FONTSIZE', (0, 1), (-1, -1), 12),
        ('TOPPADDING', (0, 1), (-1, -1), 8),
        ('BOTTOMPADDING', (0, 1), (-1, -1), 8),
    ]))
    
    elements.append(table)
    elements.append(Spacer(1, 1*cm))
    
    
    doc.build(elements)
    buffer.seek(0)
    
    return send_file(
        buffer,
        mimetype='application/pdf',
        as_attachment=True,
     
    )


@app.route("/download/csv")
def download_csv():

    result = session.get('last_calculation')
    
    if not result:
        return "No calculation result found. Please calculate first.", 400
    
    output = io.StringIO()
    writer = csv.writer(output)
    
    writer.writerow(['OSAGOCalculator Results'])

    writer.writerow([])
    
    writer.writerow(['Parameter', 'Value'])
    writer.writerow(['Final CTP Tariff (₽)', f"{result['tariff']:.0f}"])
    writer.writerow(['Final KBM', f"{result['final_kbm']:.2f}"])
    writer.writerow(['Base KBM', f"{result['base_kbm']:.2f}"])
    writer.writerow(['Region', result['region']])
    writer.writerow(['Driver', result['driver_desc']])
    writer.writerow(['Accidents', result['num_claims']])
    writer.writerow(['DTC Errors', 'Yes' if result['has_dtc'] else 'No'])
    writer.writerow([])
    
    writer.writerow(['Driver Details'])
    writer.writerow(['Age (years)', result['driver_age']])
    writer.writerow(['Experience (years)', result['driver_experience']])
    writer.writerow(['Vehicle Age (years)', result['vehicle_age']])
    writer.writerow(['Engine Power (hp)', result['engine_power']])
    
    output.seek(0)
    
    return send_file(
        io.BytesIO(output.getvalue().encode('utf-8')),
        mimetype='text/csv',
        as_attachment=True,

    )


@app.route("/download/graph")
def download_graph():

    result = session.get('last_calculation')
    
    if not result:
        return " No calculation result found. Please calculate first.", 400
    
    base_kbm = result['base_kbm']
    final_kbm = result['final_kbm']
    average_kbm = 1.0
    
    fig, ax = plt.subplots(figsize=(10, 6), facecolor='#0A1A15')
    ax.set_facecolor('#0F2319')
    
    categories = ['Base KBM', 'Final KBM', 'Average KBM']
    values = [base_kbm, final_kbm, average_kbm]
    colors = ['#00D46A', '#00A55E', '#8B8B8B']

    
    for bar, value in zip(bars, values):
        height = bar.get_height()
        ax.text(bar.get_x() + bar.get_width()/2., height,
                f'{value:.2f}',
                ha='center', va='bottom', 
                color='#E0E0E0', fontsize=12, fontweight='bold')
    
    ax.set_ylabel('KBM Coefficient', color='#A0A0A0', fontsize=12)

    ax.set_ylim(0, max(values) * 1.3)
    
    ax.grid(axis='y', alpha=0.3, color='#00D46A', linestyle='--')
    ax.set_axisbelow(True)
    
    ax.tick_params(colors='#A0A0A0')
    for spine in ax.spines.values():
        spine.set_color('#00D46A')
    
    buffer = io.BytesIO()
    plt.tight_layout()

    buffer.seek(0)
    plt.close(fig)
    
    return send_file(
        buffer,
        mimetype='image/png',
        as_attachment=True,

    )

\end{lstlisting}

% ============================================================
\newpage
\section{Приложение У}
\phantomsection
\label{app:ui_styles}
\addcontentsline{toc}{section}{Приложение У~--- CSS-стили интерфейса}
\begin{center}
\textbf{CSS-СТИЛИ ИНТЕРФЕЙСА}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Основные стили оформления}, label={lst:css}    
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]
:root {
      --bg-dark: #0a0a0a;
      --bg-card: rgba(20, 40, 30, 0.6);
      --bg-card-highlight: rgba(30, 60, 50, 0.7);
      --text-light: #e0e0e0;
      --text-muted: #a0a0a0;
      --accent-green: #00d46a;
      --accent-green-dark: #00994d;
      --border: rgba(0, 212, 106, 0.3);
      --border-highlight: rgba(0, 212, 106, 0.6);
      --input-bg: rgba(15, 25, 20, 0.5);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: black;
      color: var(--text-light);
      min-height: 100vh;
       padding: 40px 20px 0 20px;
      position: relative;
      overflow-x: hidden;
      margin: 0;
      display: flex;
      flex-direction: column;
    }

      body::before {
    content: "";
    position: fixed;  
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: 

      radial-gradient(circle at 20% 30%, rgba(0, 212, 106, 0.4) 0%, transparent 15%),
      radial-gradient(circle at 80% 20%, rgba(0, 255, 170, 0.3) 0%, transparent 12%),
      radial-gradient(circle at 60% 70%, rgba(0, 180, 120, 0.35) 0%, transparent 18%),
      radial-gradient(circle at 10% 80%, rgba(0, 220, 150, 0.25) 0%, transparent 10%),
      radial-gradient(circle at 90% 60%, rgba(0, 255, 200, 0.2) 0%, transparent 14%),
      radial-gradient(circle at 40% 50%, rgba(0, 200, 140, 0.3) 0%, transparent 16%),
      radial-gradient(circle at 30% 10%, rgba(0, 240, 180, 0.2) 0%, transparent 8%),
      radial-gradient(circle at 70% 90%, rgba(0, 210, 160, 0.25) 0%, transparent 11%),
      radial-gradient(circle at 50% 40%, rgba(0, 230, 170, 0.15) 0%, transparent 9%),
      radial-gradient(circle at 15% 50%, rgba(0, 255, 190, 0.18) 0%, transparent 13%),
      radial-gradient(circle at 85% 40%, rgba(0, 200, 150, 0.22) 0%, transparent 10%),

      radial-gradient(circle at 35% 65%, rgba(0, 240, 160, 0.3) 0%, transparent 6%),
      radial-gradient(circle at 65% 15%, rgba(0, 220, 140, 0.25) 0%, transparent 7%),
      radial-gradient(circle at 90% 85%, rgba(0, 255, 180, 0.2) 0%, transparent 5%),
      radial-gradient(circle at 5% 40%, rgba(0, 210, 150, 0.28) 0%, transparent 8%),
      radial-gradient(circle at 55% 95%, rgba(0, 230, 170, 0.18) 0%, transparent 6%);
    filter: blur(20px); 
    pointer-events: none;
    z-index: -2;
  }

    body::after {
      content: "";
      position: fixed;  
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: 
        radial-gradient(circle at 25% 35%, rgba(0, 255, 200, 0.5) 0%, transparent 8%),
        radial-gradient(circle at 75% 65%, rgba(0, 230, 170, 0.4) 0%, transparent 7%),
        radial-gradient(circle at 45% 80%, rgba(0, 240, 180, 0.35) 0%, transparent 6%),
        radial-gradient(circle at 80% 10%, rgba(0, 255, 190, 0.3) 0%, transparent 5%);
      filter: blur(15px);  
      pointer-events: none;
      z-index: -1;
    }

\end{lstlisting}

% ============================================================
\newpage
\section{Приложение Ф}
\phantomsection
\label{app:html_form}
\addcontentsline{toc}{section}{Приложение Ф~--- HTML-структура формы}
\begin{center}
\textbf{HTML-СТРУКТУРА ФОРМЫ ВВОДА}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Разметка формы калькулятора}, label={lst:html}    
    breakatwhitespace=true,
    breakindent=10pt,
    columns=fullflexible
]
<div id="driving-style-section" class="form-section" style="display: none;">
      <h2 class="section-title">Driving style Indicators</h2>
      <div class="form-grid">

        <div class="form-group"><label>Average speed (km/h)</label>
            <input type="number" name="avg_speed">
        </div>

        <div class="form-group"><label>Maximum speed (km/h)</label>
            <input type="number" name="max_speed">
        </div>
   
        <div class="form-group"><label>Sudden braking</label>
            <input type="number" name="sudden_braking">
        </div>
        
        <div class="form-group"><label>Average speed (km/h)</label>
            <input type="number" name="avg_speed_2">
        </div>

        <div class="form-group"><label>Maximum speed (km/h)</label>
            <input type="number" name="max_speed_2">
        </div>
  ъ
      </div>
    </div>
\end{lstlisting}

% ============================================================
\newpage
\section{Приложение Х}
\phantomsection
\label{app:kbm_graph}
\addcontentsline{toc}{section}{Приложение Х~--- График сравнения КБМ}
\begin{center}
\textbf{ГРАФИК СРАВНЕНИЯ КОЭФФИЦИЕНТОВ КБМ}
\end{center}

\vspace{1cm}

\begin{figure}[h]
\centering
\includegraphics[width=0.8\textwidth]{pictures/kbm_graph.png}
\caption{Сравнение индивидуального КБМ со среднерыночным значением}
\label{fig:kbm_comparison}
\end{figure}

% ============================================================
\newpage
\section{Приложение Ц}
\phantomsection
\label{app:csv_example}
\addcontentsline{toc}{section}{Приложение Ц~--- Пример CSV-файла}
\begin{center}
\textbf{ПРИМЕР CSV-ФАЙЛА С РЕЗУЛЬТАТАМИ}
\end{center}

\vspace{1cm}

\begin{lstlisting}[caption={Структура экспортируемого CSV-файла}, label={lst:csv}]
OSAGOCalculator Results
Generated,2026-04-21 01:23:58

Parameter,Value
Final CTP Tariff (₽),11100
Final KBM,0.55
Base KBM,0.46
Region,Moscow

Driver Details
Age (years),40
Experience (years),20
Vehicle Age (years),6
Engine Power (hp),150
Number of Claims,0
DTC Errors,No
\end{lstlisting}
